import 'package:flutter/material.dart';

class View2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Welcome Second")),
    );
  }
}
